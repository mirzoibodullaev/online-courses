export interface Course {
    id: number;
    title: string;
    image: string;
    description: string;
    rating: number;
}

export interface Review {
    id: number;
    review: string;
    image: string;
}
